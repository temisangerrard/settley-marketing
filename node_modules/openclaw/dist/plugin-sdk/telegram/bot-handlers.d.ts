import { RegisterTelegramHandlerParams } from "./bot-native-commands.js";
export declare const registerTelegramHandlers: ({ cfg, accountId, bot, opts, telegramFetchImpl, runtime, mediaMaxBytes, telegramCfg, allowFrom, groupAllowFrom, resolveGroupPolicy, resolveTelegramGroupConfig, shouldSkipUpdate, processMessage, logger, }: RegisterTelegramHandlerParams) => void;
